#include <stdio.h>

int main(){

    int num;
    printf("Enter an integer");
    scanf("%d", &num);

    if(num >40){
        printf("The number is greater than 40\n");
    }
    else{
        printf("The number is less than 40\n");

    }

return 0;
}